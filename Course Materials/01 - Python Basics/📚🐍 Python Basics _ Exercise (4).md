# Exercise

## Calculations with Variables

Now that you've learned how to create variables, it's time to use them in calculations.

Instead of working directly with numbers, you can perform mathematical operations using variables.

How many total hours would you spend studying in **3 weeks** if you study **5 hours each week**?

---

## Instructions

- Create a variable `weekly_hours` with the value `5` and a variable `num_weeks` with the value `3`.
- Multiply `weekly_hours` by `num_weeks` and store the result in a variable named `total_hours`.
- Print the value of `total_hours`.

---

> [!TIP]
> Variables can be used in mathematical expressions just like regular numbers.

---

## Starter Code

```python
# Create the variables weekly_hours and num_weeks



# Multiply weekly_hours and num_weeks


# Print total_hours


```

---

> [!NOTE]- Reference Solution (Instructor Only)

```python
# Create the variables weekly_hours and num_weeks
weekly_hours = 5
num_weeks = 3

# Multiply weekly_hours and num_weeks
total_hours = weekly_hours * num_weeks

# Print total_hours
print(total_hours)
```

---

> [!SUCCESS] Expected Output

```text
15
```