# Exercise

## Create a List

A **list** is a compound data type that allows you to store multiple values in a single variable.

For example:

```python
a = "is"
b = "nice"
my_list = ["my", "list", a, b]
```

After tracking the prices of your favorite shopping items, you decide to store them together in a single list.

The prices are already stored in separate variables.

---

## Instructions

- Create a list named `prices` containing the values of `book`, `shirt`, `shoes`, `watch`, and `bag` in this exact order.
- Print the `prices` list using `print()`.

---

> [!TIP]
> Create a list by placing values inside square brackets `[]`, separated by commas.

---

## Starter Code

```python
book = 15.50
shirt = 25.0
shoes = 45.99
watch = 80.0
bag = 35.25

# Create list prices


# Print prices


```

---
