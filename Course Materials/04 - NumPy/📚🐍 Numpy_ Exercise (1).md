# Exercise

## Creating Your First NumPy Array

In data science, you'll often work with large collections of numerical data. While Python lists are useful, the **NumPy** package provides a powerful alternative called a **NumPy array**, which enables fast and efficient numerical computations.

In this exercise, you'll convert a regular Python list of player weights into a NumPy array.

---

## Instructions

- Import the `numpy` package as `np`.
- Use `np.array()` to create a NumPy array from `weights`. Name this array `np_weights`.
- Print the type of `np_weights` to verify that it is a NumPy array.

---

> [!TIP]
> Use the `np.array()` function to convert a regular Python list into a NumPy array.

---

## Starter Code

```python
# Import the numpy package as np


weights = [75, 82, 88, 91, 79, 84, 90, 73]

# Create a numpy array from weights: np_weights


# Print out type of np_weights
```

---
